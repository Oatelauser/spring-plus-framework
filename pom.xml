<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.oatelauser</groupId>
    <artifactId>spring-plus-framework</artifactId>
    <version>1.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>spring-plus-framework</name>
    <description>Spring Web / Spring Boot 能力拓展公共库：统一响应、全局异常处理（JSON/SSE/NDJSON）、流式响应、服务治理（幂等防重）、声明式鉴权、内存 SQL 查询</description>
    <url>https://github.com/Oatelauser/spring-plus-framework</url>

    <modules>
        <module>spring-plus-web-starter</module>
        <module>spring-plus-boot-starter</module>
        <module>spring-plus-redis-starter</module>
        <module>spring-plus-governor-starter</module>
        <module>spring-plus-security-starter</module>
        <module>spring-plus-calcite-memory</module>
        <module>examples/spring-boot-web-example</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-boot.version>4.1.1</spring-boot.version>
        <lombok.version>1.18.46</lombok.version>
        <mybatis-flex.version>1.11.8</mybatis-flex.version>
        <calcite.version>1.42.0</calcite.version>
        <swagger.version>2.2.53</swagger.version>
        <hibernate-validator.version>9.1.3.Final</hibernate-validator.version>
        <jmh.version>1.37</jmh.version>
    </properties>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>Oatelauser</id>
            <name>Oatelauser</name>
            <url>https://github.com/Oatelauser</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/Oatelauser/spring-plus-framework</url>
        <connection>scm:git:https://github.com/Oatelauser/spring-plus-framework.git</connection>
        <developerConnection>scm:git:git@github.com:Oatelauser/spring-plus-framework.git</developerConnection>
    </scm>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM：只借版本管理，不继承 starter-parent，避免与消费方 parent 冲突 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 内部模块 -->
            <dependency>
                <groupId>io.github.oatelauser</groupId>
                <artifactId>spring-plus-web-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.oatelauser</groupId>
                <artifactId>spring-plus-boot-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.oatelauser</groupId>
                <artifactId>spring-plus-governor-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.oatelauser</groupId>
                <artifactId>spring-plus-security-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.oatelauser</groupId>
                <artifactId>spring-plus-calcite-memory</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- 内存 SQL 引擎 -->
            <dependency>
                <groupId>org.apache.calcite</groupId>
                <artifactId>calcite-core</artifactId>
                <version>${calcite.version}</version>
            </dependency>

            <!-- MyBatis 独立会话接入（calcite-memory optional 集成） -->
            <dependency>
                <groupId>com.mybatis-flex</groupId>
                <artifactId>mybatis-flex-core</artifactId>
                <version>${mybatis-flex.version}</version>
            </dependency>

            <!-- 接口文档注解（examples 使用） -->
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger.version}</version>
            </dependency>

            <!--
              ClassValidatorPostProcessor 反射依赖 hibernate-validator 内部 API
              （ValidatorFactoryImpl#beanMetaDataManagers / LocalValidatorFactoryBean#setTargetValidator），
              HV 大版本升级（9.x → 10.x）可能直接破坏该集成。
              此处显式锁定已验证版本，升级 Boot 时若 HV 跳大版本需同步回归 validation 域测试。
            -->
            <dependency>
                <groupId>org.hibernate.validator</groupId>
                <artifactId>hibernate-validator</artifactId>
                <version>${hibernate-validator.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 公共库卫生：全局 dependencies 一概不下发，各模块按需声明；lombok 只在编译期存在 -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.16.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <!-- Spring 7 移除 LocalVariableTableParameterNameDiscoverer：
                             保留字节码参数名，@RequestParam/@PathVariatle 按名绑定依赖此配置 -->
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.6.0</version>
                    <configuration>
                        <argLine>-Dfile.encoding=UTF-8</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <configuration>
                        <doclint>none</doclint>
                        <charset>UTF-8</charset>
                        <encoding>UTF-8</encoding>
                        <docencoding>UTF-8</docencoding>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!-- 发布到 Maven 中央仓库时手动激活：mvn -P release deploy -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.8.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <!-- 不发布的模块（按 artifactId 匹配，含其 sources/javadoc 附属构件）：
                                 示例模块缺 sources/javadoc 会令 bundle 整体校验被拒；
                                 calcite-memory 自 1.0.1 起停止发布 Central（源码保留，本地/私仓引入） -->
                            <excludeArtifacts>
                                <artifactId>spring-boot-web-example</artifactId>
                                <artifactId>spring-plus-calcite-memory</artifactId>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
            <distributionManagement>
                <snapshotRepository>
                    <id>central</id>
                    <url>https://central.sonatype.com/repository/maven-snapshots/</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>
    </profiles>

</project>
