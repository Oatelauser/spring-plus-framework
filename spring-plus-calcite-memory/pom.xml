<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.oatelauser</groupId>
        <artifactId>spring-plus-framework</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>spring-plus-calcite-memory</artifactId>
    <name>spring-plus-calcite-memory</name>
    <description>基于 Apache Calcite 的内存表 SQL 查询框架：将 POJO/Map/List 注册为内存临时表并提供 SQL 查询能力</description>

    <dependencies>
        <!-- 内存 SQL 引擎 -->
        <dependency>
            <groupId>org.apache.calcite</groupId>
            <artifactId>calcite-core</artifactId>
        </dependency>

        <!-- MyBatis 独立会话接入：仅用 org.apache.ibatis.* 原生 API 构建 SqlSessionFactory。
             optional：消费方启用 MyBatis 集成时自行引入 -->
        <dependency>
            <groupId>com.mybatis-flex</groupId>
            <artifactId>mybatis-flex-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 本模块仅做编译期断言：Spring 注解可选依赖，自动配置阶段启用 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>

        <!-- 指标桥接：Micrometer（SimpleMeterRegistry/Timer/Counter）。optional：消费方带 actuator/prometheus 时启用 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 请求级 Session 绑定：RequestContextHolder / @RequestScope。optional：仅 Web 应用启用 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- 测试：ApplicationContextRunner / SimpleMeterRegistry / MockHttpServletRequest -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 测试：jakarta Servlet API，MockHttpServletRequest 间接需要（spring-test 中为 optional，不传递） -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- JMH 基准：@Benchmark / Runner / Blackhole 运行期 API -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 覆盖根 POM 的 annotationProcessorPaths：在 Lombok 之外追加 JMH 注解处理器，
                 使 @Benchmark 生成 BenchmarkList 与可执行 Runner 入口。combine.self=override 完整替换继承列表。 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths combine.self="override">
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.openjdk.jmh</groupId>
                            <artifactId>jmh-generator-annprocess</artifactId>
                            <version>${jmh.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
